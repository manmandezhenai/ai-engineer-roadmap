# Day 11 - while 循环与循环控制

## 学到的概念

### 1.`while` 循环的基本结构

```python
while 条件:
    条件为True时重复执行的代码块
```

- `while` 循环在**条件为真时一直重复执行**，一旦条件为假就停止
- **关键**：必须在循环体内修改条件中涉及的变量，否则循环永远不会停止（死循环）

### 2.循环控制

- `break`：立即跳出整个循环
- `continue`：跳过本次循环剩余代码，进入下一次循环