# Day 5 - Python 安装与第一行代码

## 学到的概念

- Python 解析器：执行 `.py` 文件的程序
- `input()` 函数：让用户输入数据，**返回字符串**

## 学会的操作

### 1.安装 Python

- **下载地址**：https://www.python.org/downloads/
- **重要**：安装时务必勾选 **“Add Python to PATH”**
- **验证安装**：在终端输入 `python --version`，如果显示 `Python 3.xx.x` 即为成功。